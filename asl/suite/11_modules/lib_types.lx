-- lib_types.lx
-- Exported type definitions for module import tests

+Color = | Red | Green | Blue

+color_name = (c) c ? {
  Red   -> "red"
  Green -> "green"
  Blue  -> "blue"
}

+Pair a b = {fst: a  snd: b}

+make_pair = (a b) {fst: a  snd: b}

+swap_pair = (p) {fst: p.snd  snd: p.fst}
