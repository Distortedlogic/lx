graph TD
    start(["0.0.1 Start"]):::teal
    start --> guardrail["0.0.2 Commit baseline\n——————\ngit add -A && git commit\n--allow-empty -m baseline\ngit push"]:::cyan
    guardrail --> loop_start["0.1.0 For each group"]:::purple

    loop_start --> implement["`**1.0.0 Implement (Spawn Subagent)**
——— read ———
every task in the work item
———
⟳ for each task {
→ do it fully, in order
→ do not skip or mark done early
→ do not combine with other tasks
→ do not add work not in the task
→ do not substitute a different approach
→ do not touch files outside the task scope
→ do not add infrastructure not in the spec
}
———
⟳ if your change creates a new problem {
→ fix the root cause in this task
→ do not restructure other code to avoid it
}
⟳ if you must deviate from the spec {
→ stop · document why · before proceeding
}`"]:::orange
    implement --> audit["`**2.0.0 Audit (Spawn Subagent)**
——— read ———
work item spec · git diff of all changes
(git diff — changes are uncommitted)
———
⟳ for each task in work item {
→ read what the task says to do
→ read what the diff actually did
→ did it do what was asked?
→ did it do anything not asked?
→ did it leave anything incomplete?
}
——— check all touched files ———
empty function bodies
functions returning placeholders
match arms discarding with _ => {}
new struct/trait/enum with no users
TODO/FIXME/unimplemented left behind
unused imports · dead functions · orphaned consts
pub items nothing outside the module uses
code compiles with 0 warnings
all tests pass
——— check for failure modes ———
workarounds: restructured code to dodge a bug?
regressions: new ambiguities or edge cases?
scope creep: files outside the work item touched?
deviations: spec vs actual — justified?
——— scope ———
grade ONLY current file state on disk
do not grade commit history or staging
the commit step happens AFTER audit passes
——— grade 0 if ———
any pre-existing test regresses
implementer worked around own bug
——— returns ———
grade 0–100
list of {file, issue, severity}`"]:::yellow
    audit --> check{{"2.1.0 grade >= 93?"}}:::teal

    check -- "no" --> stuck{{"2.2.0 stuck?\n——————\n3+ attempts ·\ngrade not improving ·\nsame findings repeating"}}:::red
    check -- "yes" --> diagnose["2.0.1 just diagnose"]:::cyan
    diagnose --> diag_check{{"2.3.0 errors or\nwarnings?"}}:::teal
    diag_check -- "no" --> fmtcommit["3.0.1 fmt + commit + push"]:::teal
    diag_check -- "yes" --> fix
    fmtcommit --> more{{"3.1.0 more groups?"}}:::purple

    stuck -- "no" --> fix["`**3.0.0 Fix (Spawn Subagent)**
——— input ———
findings list from auditor
———
⟳ for each finding {
→ open the flagged file
→ fix exactly the issue described
→ fix root causes, not symptoms
→ if finding is a workaround, fix the
  underlying problem
→ do not introduce new workarounds
→ do not add features
→ do not refactor nearby code
→ do not touch files not in findings
}`"]:::orange
    fix --> audit

    stuck -- "yes" --> bail["`**3.0.2 Bail**
——— log ———
which group failed (if applicable)
final grade
every unresolved finding`"]:::red
    bail --> done

    more -- "yes" --> loop_start
    more -- "no" --> sys_audit["`**4.0.0 System Audit (Spawn Subagent)**
——— phase 1: code quality ———
⟳ for each file in codebase {
→ empty function bodies
→ functions returning placeholders
→ match arms discarding with _ => {}
→ pub items with no external users
→ TODO/FIXME/unimplemented left behind
}
code compiles with 0 warnings
all tests pass
——— phase 2: spec compliance ———
⟳ for each work item that was executed {
→ read the work item spec
→ read the committed diff for that group
→ for each task in the work item {
→→ did the diff do what the task asked?
→→ did it leave anything incomplete?
→→ did it add anything not in the task?
  }
}
——— phase 3: cross-item integrity ———
⟳ for each pair of work items {
→ did a later item work around a problem
  introduced by an earlier item?
→ if so: fix at the source, not downstream
}
⟳ for each behavioral change {
→ is it tested?
→ undocumented edge cases = finding
}
——— returns ———
grade 0–100
list of {file, issue, severity}
(includes code quality, spec compliance,
and cross-item findings)`"]:::cyan
    sys_audit --> sys_check{{"4.1.0 grade >= 93?"}}:::teal

    sys_check -- "no" --> sys_stuck{{"4.2.0 stuck?\n——————\n3+ attempts ·\ngrade not improving ·\nsame findings repeating"}}:::red
    sys_check -- "yes" --> sys_fmtcommit["4.0.1 fmt + commit + push"]:::teal
    sys_fmtcommit --> done(["4.0.2 Done"]):::teal

    sys_stuck -- "no" --> sys_fix["`**4.3.0 Fix (Spawn Subagent)**
——— input ———
findings list from auditor
———
⟳ for each finding {
→ open the flagged file
→ fix exactly the issue described
→ fix root causes, not symptoms
→ if finding is a workaround, fix the
  underlying problem
→ do not introduce new workarounds
→ do not add features
→ do not refactor nearby code
→ do not touch files not in findings
}`"]:::orange
    sys_fix --> sys_audit
    sys_stuck -- "yes" --> bail
