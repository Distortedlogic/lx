graph TD
    start(["Start"]):::teal
    start --> loop_start["For each group"]:::purple

    loop_start --> implement["`**1. Implement**
read every task in the work item
do each task fully, in order, one at a time
do not skip or mark done early
do not combine tasks
do not add work not in the task
do not substitute a different approach`"]:::orange
    implement --> audit["`**2. Audit**
——— read ———
work item spec · diff of all changes
——— for each task ———
did the code change match the spec?
——— check all touched files ———
empty function bodies
functions returning placeholders
match arms discarding with _ => {}
new struct/trait/enum with no users
TODO/FIXME/unimplemented left behind
code compiles with 0 warnings
all tests pass
——— returns ———
grade 0–100
list of {file, issue, severity}`"]:::yellow
    audit --> check{{"grade >= 93?"}}:::teal

    check -- "no" --> stuck{{"stuck?\n3+ attempts ·\ngrade not improving ·\nsame findings repeating"}}:::red
    check -- "yes" --> more{{"more groups?"}}:::purple

    stuck -- "yes" --> bail["`**Bail**
log which group failed
log final grade
log every unresolved finding`"]:::red
    bail --> done

    more -- "yes" --> loop_start
    more -- "no" --> sys_audit

    stuck -- "no" --> fix["`**3. Fix**
receive findings list from auditor
open each flagged file
fix exactly the issue described
do not add features
do not refactor nearby code
do not touch files not in findings`"]:::orange
    fix --> audit

    sys_audit["`**4. System Audit**
——— read ———
scan every file in the codebase
——— check all files ———
empty function bodies
functions returning placeholders
match arms discarding with _ => {}
pub items with no external users
TODO/FIXME/unimplemented left behind
code compiles with 0 warnings
all tests pass
——— returns ———
grade 0–100
list of {file, issue, severity}`"]:::cyan
    sys_audit --> sys_check{{"grade >= 93?"}}:::teal

    sys_check -- "no" --> sys_stuck{{"stuck?\n3+ attempts ·\ngrade not improving ·\nsame findings repeating"}}:::red
    sys_check -- "yes" --> done(["Done"]):::teal

    sys_stuck -- "no" --> sys_fix["`**5. Fix**
receive findings list from auditor
open each flagged file
fix exactly the issue described
do not add features
do not refactor nearby code
do not touch files not in findings`"]:::orange
    sys_fix --> sys_audit

    sys_stuck -- "yes" --> sys_bail["`**Bail**
log final grade
log every unresolved finding`"]:::red
    sys_bail --> done
