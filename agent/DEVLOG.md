-- Memory: journal. Design decisions, architectural debt, and session history.
-- Append-only. Add a session entry every tick. Never delete history.

# lx Development Log

## Key Design Decisions

Non-obvious choices that cause confusion if forgotten:

- **Function body extent** — `map (x) x * 2 | sum` gives map body `x * 2 | sum`. Use blocks or sections.
- **Tuple auto-spread**: N-param function + single N-tuple → auto-destructure.
- **Application requires callable left-side**. `[1 2 3]` is three elements, not application.
- **Collection-mode**: inside `[]`, only TypeConstructor triggers application.
- **Default params reduce effective arity**. 1 required arg + defaults → executes immediately.
- **Tuple variables need commas**. `(b, a)` tuple. `(b a)` is application.
- **`{x: (f 42)}`** — parens for function calls in single-line record field values (multiline records support full expressions).
- **is_func_def ambiguity**: `(a b c) (expr)` with all bare Ident params NOT a func def. Defaults/underscores/type annotations override. Session 64 added `application_depth` tracking — in application context with 2+ bare-ident params and no strong signals, rejects func-def when body is a record literal or non-matching identifier.
- **`yield` is callback-based**. No handler → runtime error. Subsumed by `RuntimeCtx.yield_` backend.
- **`RuntimeCtx` for backend pluggability**. All I/O builtins receive `&Arc<RuntimeCtx>`. Traits: `AiBackend`, `EmitBackend`, `HttpBackend`, `ShellBackend`, `YieldBackend`, `LogBackend`, `UserBackend`. Defaults in `backends/defaults.rs` + `backends/user.rs`.
- **`with ... as` uses `stop_ident` parser flag**. Expression parsing stops at `Ident("as")` to prevent it being consumed as application argument. Multi-resource separated by `,` (Semi token). Cleanup via `Closeable` convention (record with `.close` field).
- **`MCP` declarations are typed tool contracts**. Callable → record of wrapper functions.
- **Type annotations don't consume lowercase idents as type args**. `(x: Maybe a)` treats `a` as next param, not type var. Write `(x: (Maybe a))`.
- **`{` after type tokens is body, not record type**. `-> Int { body }` — `{` starts body. Record return types need parens: `-> ({x: Int})`.
- **Protocol `where` constraints bind the field name**. `score: Float where score >= 0.0`.
- **Trait spread overrides**: later fields override same-named spread fields.
- **`Trait` has typed methods**. `Trait Name = { method: {input} -> output }` — methods use MCP tool signature syntax (`{fields} -> type`). Reserved fields: `description` (Str), `requires` ([symbols]), `tags` ([Str]). Conformance validated at Agent definition time — missing method = hard runtime error. `Value::Trait` holds `methods: Vec<TraitMethodDef>` (same shape as `McpToolDef`). `std/trait` module provides `trait.methods` (extract signatures as records) and `trait.match` (keyword matching).
- **`agent.implements` is structural**. Checks method names against Trait's declared methods. Works with `Value::Class`, `Value::Object`, and `Value::Record`. Falls back to `__traits` string tags for empty-method traits. No `?` suffix — parses as ternary operator.
- **`std/profile` uses `DashMap` + atomic IDs**. Same pattern as `std/knowledge`. File-backed at `.lx/profiles/{name}.json`. Strategy helpers use `strategy:{problem}:{approach}` domain prefix convention.
- **`Agent` is a keyword + Trait**. Lexed like `Trait`/`Trait` (uppercase → TypeName special-case). Produces `Stmt::AgentDecl` and `Value::Class { name, traits, defaults, methods }`. The `Agent` keyword auto-imports `pkg/agent {Agent}` and auto-adds "Agent" to the traits list. `pkg/agent.lx` defines `Trait Agent` with real defaults: init, perceive, reason, act, reflect, handle (auto-dispatch by msg.action via `method_of`), run (yield/loop message loop), think/think_with/think_structured (AI), use_tool/tools (tool hooks), describe (self-description via `methods_of`), ask/tell (inter-agent communication). `init`/`on` go into the methods map; `uses` dropped. Two new builtins: `method_of(obj, name)` and `methods_of(obj)`. Display checks traits list for "Agent" → `<Agent X>` vs `<Class X>`.
- **`Trait` is a `Value::Trait` with non-empty fields**. Session 64: `Value::Trait` eliminated — Trait declarations produce `Value::Trait` where `fields` is non-empty. Callable as constructor (Protocol semantics). Display: `<Trait X>` for Traits-with-fields, `<Trait X>` for behavioral Traits.
- **`Store` is a first-class Value type**. `Value::Store { id }` with dot-access methods (set, get, keys, values, entries, remove, len, has, clear, filter, query, map, update, save, load, persist, reload). `Store ()` constructor. Reference semantics. Object fields backed by STORES DashMap (same as Store values). Session 64: OBJECTS DashMap eliminated.

## Technical Debt

Architectural constraints that inform design decisions. Not actionable bugs (those are in `BUGS.md`).

- **Async interpreter shipped (71d)** — `eval` is `async fn` with `#[async_recursion(?Send)]`. `par`/`sel`/`pmap` use `futures::join_all`/`select_all`. Rayon dependency still present but unused by concurrency primitives. Follow-up: remove rayon, make backend traits async, convert remaining 31 `call_value_sync` stdlib callers to async builtins.
- Currying removal deferred — requires parser architecture change
- `it` in `sel` blocks — only implicit binding, no explicit name
- Shell line is single-line only — forces `${ }` for multi-line commands
- Named args + default params + currying have complex interaction edge cases

## Session History

| # | Date | Focus |
|---|------|-------|
| 1-5 | 03-13 | Spec authoring, contradiction fixes, test files |
| 6-7 | 03-13 | First Rust impl — lexer, parser, interpreter, type defs |
| 8-14 | 03-14 | Parser fixes, iterators, shell, concurrency, modules, agents, Trait |
| 15-18 | 03-14 | Stdlib (json/ctx/math/fs/env/re/md/agent/mcp), MCP HTTP transport |
| 19 | 03-14 | Removed 7 features (regex, $$, <>, sets, iterators, types, tuple semis) |
| 20-24 | 03-14 | yield, MCP decls, std/http, std/time, file splits, with/field update |
| 25 | 03-14 | Stale spec cleanup — all 22 spec files updated |
| 26 | 03-14 | std/cron, real-flow gap analysis vs mcp-toolbelt arch_diagrams |
| 27 | 03-15 | Repo reorg (asl/ -> spec/design/tests/flows), 14 flow programs + specs |
| 28 | 03-15 | Design review: types + regex back, full stdlib roadmap |
| 29 | 03-15 | Type annotations + checker: AST, parser, bidirectional inference, `lx check` |
| 30 | 03-15 | Regex literals: `r/\d+/flags`, Value::Regex, std/re accepts both, 25/25 tests |
| 31 | 03-15 | Agentic features: `~>>?` streaming, checkpoint/rollback, capabilities, blackboard, events, negotiation |
| 32 | 03-15 | Agentic layer completion: dialogue, interceptors, handoff, plan revision, introspection, knowledge cache |
| 33 | 03-15 | std/ai + std/tasks + std/audit + std/circuit + std/knowledge + std/plan + std/introspect. 19 stdlib modules, 32/32 tests |
| 34 | 03-15 | Agent self-assessment: 10 missing features identified. 8 new spec files + updates to 10 existing files |
| 35 | 03-15 | Standard agents (auditor/router/grader/planner/monitor/reviewer) + std/memory + std/trace. 40/40 tests |
| 36 | 03-15 | std/diag: program visualization. AST walker, Mermaid output. `lx diagram` CLI. 41/41 tests |
| 37 | 03-15 | RuntimeCtx design + implementation. Backend traits. Refine expression. agent.reconcile. 44/44 tests |
| 38 | 03-15 | trace.improvement_rate + trace.should_stop: diminishing returns detection. 45/45 tests |
| 39 | 03-15 | Agent communication layer: dialogue, intercept, handoff, capabilities, gate, supervise, mock, dispatch, ai.prompt_structured. 54/54 tests |
| 40 | 03-15 | Trait extensions: composition, unions, field constraints. 56/56 tests |
| 41 | 03-16 | `with ... as` scoped resources, `Trait` declarations + `agent.implements`, `std/pool`. 59/59 tests |
| 42 | 03-16 | Tier 1 stdlib (`std/budget`, `std/prompt`, `std/context`) + Tier 2 agent extensions (`agent.negotiate`, `agent.topic`/`subscribe`/`publish` pub/sub). 64/64 tests |
| 43 | 03-16 | `std/git`: structured git access — 36 functions (status, log, diff, blame, grep, add, commit, branch, stash, remote). 7 Rust files, unified diff parser. 65/65 tests |
| 44 | 03-16 | Gap analysis: 7 new specs for unplanned features (profile, interrupt, constraint propagation, provenance, workspace, teaching, pipeline checkpoint). Priority queue restructured. `std/blackboard`/`std/events` eliminated |
| 45 | 03-16 | `std/retry`: retry-with-backoff (2 functions, `fastrand` dep for jitter). Improved binding pattern-match error messages. 66/66 tests |
| 46 | 03-16 | Spec consolidation: 9 merges applied. Eliminated `std/strategy` (→ `std/profile`), `std/reputation` (→ `std/trace`), `checkpoint`/`on_interrupt` keywords (→ `user.check` + `:signal` lifecycle hook), `plan.run_incremental` (→ `std/pipeline`), `agent.teach` (→ dialogue convention), `workflow.peers` (→ topic convention), constraint propagation spec (→ `with context` ambient), provenance spec (→ `std/trace`), `Goal`/`Task` (→ docs). Agent/Trait declaration specs from Session 45b integrated. Net: 21 planned features (down from ~33) |
| 47 | 03-16 | Error message overhaul: cross-language keyword hints (30+ keywords), value/type in all mismatch errors, Pattern Display impl, Value::short_display(). `cargo install` to host. Quick syntax reference in NEXT_PROMPT for cold-start agents. 66/66 tests |
| 48 | 03-16 | Gap analysis: 7 unplanned features for dynamic multi-agent coordination. New specs: `agents-task-graph` (DAG execution), `agents-capability-routing` (declarative routing), `agents-deadline` (time propagation), `agents-introspect-live` (system observation), `agents-dialogue-branch` (fork/compare/merge), `agents-format-negotiate` (Trait adapters), `agents-hot-reload` (handler swap). Updated ROADMAP (28 features), PRIORITIES (28 items), OPINION (8 new gaps). No code changes |
| 49 | 03-16 | `std/user`: 9 functions, `UserBackend` trait. `std/profile`: 15 functions, persistent identity + strategy helpers. `Agent` declarations: new `AgentKw` token, `Stmt::AgentDecl` AST node, `Value::Agent` variant. Parser handles `Agent Name: TraitList = { body }` with `uses`/`init`/`on` reserved fields. Trait conformance validated at definition time. Method access via `.`. 69/69 tests |
| 50+ | 03-17 | Flow testing infrastructure: `std/test` (test runner, test/describe blocks), `std/describe` (BDD-style describe/it with structured results). Flow satisfaction test suites for 14 flow specs — 11 deterministic suites (35 scenarios) + 3 live-only stubs. Discovered and documented 16 findings in FLOW_TESTING_FINDINGS.md. Fixed: `Trait +Name` syntax, `refine` initial expression parsing, `trace.record` Int score handling. 71/71 tests |
| 51 | 03-17 | Enforced Trait methods: `Trait Name = { method: {input} -> output }` with typed MCP-style signatures. `TraitMethodDecl` AST node, `TraitMethodDef` runtime value. Agent conformance checks method existence at definition time. `agent.implements` now structural (checks methods, not string tags). `std/trait` module: `trait.methods` (extract signatures) + `trait.match` (keyword matching). Trait-named inputs supported (`method: ProtoName -> output`). Old `handles`/`provides` syntax removed. 71/71 tests |
| 52 | 03-18 | Brain-driven language improvements (10 fixes): `/` returns Float for Int/Int (Python 3), `//` for integer division. Map/Agent field miss → `None` (uniform with Record). Trait validation → `Err` values (catchable). Record spread allows fn calls (`{..mk () ...}`). Agent `uses`/`on` wired to runtime (`Value::Agent` gains fields). `receive` keyword for agent msg loops. `ai.prompt_json` lightweight structured output. Brain sweep: removed 14 `to_float`, converted 5 agents to `receive`. 71/71 tests |
| 53 | 03-18 | Workspace Phase 1: `lx.toml` manifest parsing (`WorkspaceManifest`/`PackageManifest`), workspace discovery (walk up from cwd), `lx test` workspace-aware (iterate members, per-member results), `lx test -m name` filter, `lx list` member summary. CLI refactored: `main.rs` split into 6 files (main, manifest, testing, listing, run, agent_cmd). `toml` crate added. `lx.toml` files for tests/, brain/, workgen/, flows/. Confirmed unicode crash already fixed (deleted from BUGS.md). 71/71 tests |
| 54 | 03-18 | Workspace Phase 2: Module resolver gains workspace step — `use brain/protocols` resolves via member name (between relative and stdlib in resolution order). `lx run member-name` resolves to member's `entry` field from manifest. `lx check` gains workspace iteration (`lx check` = all members, `lx check -m name` = one member). `RuntimeCtx.workspace_members` carries member→dir map. Test and run commands auto-populate workspace members. 72/72 tests |
| 55 | 03-18 | `std/pipeline` checkpoint/resume: 8 functions (`create`, `stage`, `complete`, `status`, `invalidate`, `invalidate_from`, `clean`, `list`). Stage-boundary caching with input-hash invalidation. File-backed storage (`.lx/pipelines/<name>/`). JSON serialization via `std/json_conv`, `DefaultHasher` for input hashing. Split into `pipeline.rs` (293 lines) + `pipeline_io.rs` (99 lines). 73/73 tests |
| 56 | 03-18 | `AgentErr` structured errors: 11 tagged error variants (Timeout, RateLimited, BudgetExhausted, ContextOverflow, Incompetent, Upstream, PermissionDenied, TraitViolation, Unavailable, Cancelled, Internal). TaggedCtor constructors exposed via `use std/agent {Timeout ...}`. Rust helpers in `agent_errors.rs`. Stdlib migration: `budget.spend` → `BudgetExhausted`, `agent.kill`/`agent.ask` → `Unavailable`, `mcp.call` → `Upstream`, `pool` errors → `Internal`, HTTP backend → `Upstream`. Split `mcp.rs` → `mcp.rs` + `mcp_typed.rs`. 74/74 tests |
| 57 | 03-18 | `std/flow` composition: 8 functions (`load`, `run`, `pipe`, `parallel`, `branch`, `with_retry`, `with_timeout`, `with_fallback`). Flows as first-class composable values — load .lx files, execute in isolated interpreter, compose with sequential pipe/parallel/branch/decorators. `par` renamed to `parallel` (keyword conflict). Also marked `std/test` satisfaction testing as shipped (already implemented in Session 50+). 3 Rust files (flow.rs, flow_run.rs, flow_compose.rs). 75/75 tests |
| 58 | 03-18 | `std/taskgraph` DAG execution: 9 functions (`create`, `add`, `remove`, `run`, `run_with`, `validate`, `topo`, `status`, `dot`). Kahn's algorithm for topological sort + wave-based parallel execution. Supports: dependency resolution, `input_from` result threading, static input, handler functions, per-task retry/timeout, `on_fail` policy (fail/skip), `run_with` callbacks + `max_parallel`. DOT export for visualization. Cycle + unknown-dep detection. 3 Rust files (taskgraph.rs, taskgraph_run.rs, taskgraph_topo.rs). 76/76 tests |
| 59 | 03-18 | `std/deadline` time propagation: 8 functions (`create`, `create_at`, `scope`, `remaining`, `expired`, `check`, `slice`, `extend`). Thread-local scope stack for contextual functions. Cooperative deadline checking — agents decide how to degrade. `slice` creates sub-deadlines from percentage of remaining time. Auto-propagation: `~>?`/`~>` inject `_deadline_ms` into Record messages when inside deadline scope. 1 Rust file (deadline.rs, 243 lines). 77/77 tests |
| 60 | 03-18 | `agent.route`/`register` capability routing: 5 functions (`register`, `unregister`, `registered`, `route`, `route_multi`). Process-local routing table with trait/trait/domain filters. Selection strategies: `least_busy` (default), `round_robin`, `random`, custom function. Load tracking via atomic counters. `route_multi` fan-out with optional reconcile integration. Fallback agents, exclude lists, max_concurrent capacity. 2 Rust files (agent_route.rs + agent_route_table.rs). 78/78 tests |
| 61 | 03-18 | Bug fix session. List spread bp fix (`parse_expr(32)` → `parse_expr(0)` for consistency with non-spread elements). Agent body uppercase tokens (accept TypeName in Agent body + dot field access). Confirmed module `../../` multi-level and single-line multi-field records already worked — removed stale BUGS.md entries. Split all 7 over-300-line files into 14 files (stmt_trait, agent_reconcile_score, cron_helpers, str_extra, diag_types, walk_helpers, tasks_transition). 0 new features, 3 parser fixes, 7 file splits. 78/78 tests |
| 62 | 03-18 | Feature consolidation + `std/store` + 9 module→package migrations. Mechanical deduplication (retry/step_deps/deadline). `std/store` primitive (242 lines, 12 functions). Converted: knowledge, circuit, prompt, tasks, trace, memory, context, introspect, pool. ~-2770 lines Rust, +950 lines lx. Budget/profile/pipeline stay Rust (need dynamic field access, randomness, hashing). `trace.filter`→`trace.query`, `pool.map`→`pool.map_tasks` (builtin shadowing). Identified: lx needs generic `Class` keyword — Agent is actor-specific, not suitable for plain stateful objects. 79/79 tests |
| 63 | 03-18 | `Class` keyword + Trait default methods. `Class Name : [Traits] = { field: default; method = (params) { body } }` — generic stateful objects, Agent minus messaging. DashMap-backed global object store (`Value::Object` with u64 handle), `self.field <-` in-place mutation, reference semantics. Trait defaults: `method = (params) { body }` in Trait bodies, auto-injected into conforming Agent/Class. 8 pkg packages converted from handle-passing to Class declarations (CircuitBreaker, KnowledgeBase, TaskStore, TraceStore, MemoryStore, ContextWindow, Inspector, Pool). All flows/brain/workgen callers updated. 80/80 tests |
| 64 | 03-19 | Store promoted to first-class `Value::Store { id }` (dot-access methods, `Store ()` constructor, reference semantics, Store cloning in Class constructors). `Collection` Trait (`pkg/collection.lx`): get/keys/values/remove/query/len/has/save/load as Trait defaults delegating to `self.entries`. 5 collection packages rewritten (knowledge, tasks, memory, trace, context). Type hierarchy refactor: OBJECTS DashMap eliminated (Object fields → STORES), `Value::Agent` removed, `Value::Trait` removed (Protocol → `Value::Trait` with non-empty fields). Agent is now a Trait defined in `pkg/agent.lx` — `Agent` keyword auto-imports it and auto-adds "Agent" to traits list. `Value::Class` has 4 fields: name, traits, defaults, methods (no ClassKind, no init/on/uses fields). Two new builtins: `method_of`, `methods_of`. `is_func_def()` parser bug fix for `(expr) {record}` in application context. Boxed `LxFunc`/`FieldDecl`, visitor context structs. 80/80 tests |
| 65 | 03-19 | `std/introspect` system-wide live observation: 5 functions (`system`, `agents`, `agent`, `messages`, `bottleneck`). Aggregates from all global registries: REGISTRY (subprocess agents with new metadata — name, traits, spawn time, in-flight/completed/error counters), SESSIONS (dialogues), SUPERVISORS (restart tracking), TOPICS (pub/sub), ROUTE_TABLE (routing load). `AgentProcess` enriched with per-agent metrics. `ask_subprocess` now tracks in-flight count, completed, and errors. `introspect.watch` deferred (needs async/threading). 81/81 tests |
| 66 | 03-19 | `agent.pipeline` consumer-driven flow control with backpressure: 11 functions (`pipeline`, `pipeline_send`, `pipeline_collect`, `pipeline_batch`, `pipeline_stats`, `pipeline_on_pressure`, `pipeline_pause`, `pipeline_resume`, `pipeline_drain`, `pipeline_close`, `pipeline_add_worker`). Bounded buffers between stages, 4 overflow policies (block, drop_oldest, drop_newest, sample), tail-first pump algorithm, round-robin worker dispatch, pressure callbacks, per-stage stats with bottleneck detection. 3 Rust files (agent_pipeline.rs + agent_pipeline_io.rs + agent_pipeline_ctrl.rs). 82/82 tests |
| 67 | 03-19 | `~>>?` streaming ask: new `Value::Stream` variant (mpsc channel-backed), `Expr::StreamAsk` AST node, `TildeArrowArrowQ` token. Local agents: handler result → channel (List items streamed individually, Ok-wrapped lists unwrapped, scalars wrapped as single-element). Subprocess agents: JSON-line protocol (`stream`/`stream_end`/`stream_error` types) with background reader thread and cancellation channel. HOFs (`map`/`filter`/`each`/`take`/`fold`/`flat_map` etc.) work on streams via `get_list` extension. `collect` materializes stream into list. Agent-side: `agent.emit_stream` and `agent.end_stream` builtins. 1 new Rust file (agent_stream.rs). 83/83 tests |
| 68 | 03-19 | `pkg/trace` extensions — provenance + reputation. Provenance: `enable_provenance`, `record_hop` (msg_id/from/to/action), `message_path` (ordered hop chain), `message_hops` (count). Reputation: `agent_score` (per-agent stats from spans + hops), `agent_rank` (sorted agent leaderboard). New fields: `provenance: Store ()`, `provenance_enabled`. `record` now captures `agent` field. `query` now supports `agent` filter. Pure lx — no Rust changes. 84/84 tests |
| 69 | 03-19 | `std/workspace` collaborative editing: 12 functions (`create`, `claim`, `claim_pattern`, `edit`, `append`, `release`, `snapshot`, `regions`, `conflicts`, `resolve`, `history`, `watch`). Line-based region claiming with overlap detection, bound auto-adjustment on edit/append, regex-based pattern claiming, edit history, watcher callbacks. DashMap-backed shared state (safe for `par`/`pmap`). 2 Rust files (workspace.rs + workspace_edit.rs). 85/85 tests |
| 70 | 03-19 | `std/registry` cross-process discovery: 10 functions (`start`, `stop`, `connect`, `register`, `deregister`, `find`, `find_one`, `health`, `load`, `watch`). In-memory registry with DashMap-backed agent entries, trait/trait/domain filtering, 4 selection strategies (first, least_loaded, round_robin, random), health/load tracking, watcher callbacks for join/leave events. 3 Rust files (registry.rs + registry_query.rs + registry_store.rs). 86/86 tests |
| 71 | 03-19 | `agent.dialogue_fork`/`compare`/`merge`/`branches` dialogue branching: 4 functions. Fork creates N sessions sharing parent history, parent suspended while forks active. Compare grades forked sessions via user-supplied function, returns rankings/best/spread. Merge appends winner's post-fork history to parent, cleans up all forks (recursive tree cleanup). Branches lists active forks. DialogueSession extended with parent_id/fork_ids/suspended fields, HistoryEntry now Clone. Dialogue builtins refactored to `builtins()` pattern. 1 new Rust file (agent_dialogue_branch.rs). 87/87 tests |
| 71b | 03-19 | Five structural weakness fixes + three-domain modernization. (1) Real parallelism: `par`/`sel`/`pmap`/`pmap_n` use `std::thread::scope` for OS threads — no tokio needed. (2) `lx check` imports: checker resolves `use` stmts, binds imported names as `Type::Unknown` — false positives 122→31. (3) Export shadowing fix: `+` exports excluded from forward declarations so builtins not shadowed inside defining module. (4) `std/durable`: 8 functions for Temporal-style workflow persistence (workflow, run, step, sleep, signal, send_signal, status, list), 3 Rust files, atomic file I/O. (5) Package manager: `lx install`/`lx update` with git+path deps, `lx.lock`, `.lx/deps/` resolution, 4 new CLI files. Modernized 60+ lx files across brain/ (18 files), flows/ (40 files), workgen/ (2 files) — record shorthand, string interpolation, ternaries, pipe chains, eliminated intermediates. 90/90 tests |
| 71c | 03-19 | Upgraded concurrency from `std::thread::scope` to two-pool architecture: rayon (work-stealing CPU pool) + tokio runtime (async I/O bridge). `par`/`sel` use `ctx.rayon_pool.scope()`. `pmap`/`pmap_n` use rayon with indexed channel sends. `RuntimeCtx` gains `tokio_runtime: Arc<Runtime>` and `rayon_pool: Arc<ThreadPool>`. All RuntimeCtx constructors updated (CLI, testing, dx backend). Evaluated CGP crate for backend system — rejected (solves problems lx doesn't have, 30+ deps, steep learning curve, no production adoption). Design decision: next major milestone is async interpreter (`async fn eval`) — the endgame for proper I/O interleaving in pmap. 90/90 tests |
| 71d | 03-19 | **Async interpreter migration.** `eval`/`exec`/`eval_expr` → `async fn` with `#[async_recursion(?Send)]`. All 15 interpreter sub-files propagated. `par` → `futures::future::join_all`, `sel` → `futures::future::select_all`, `pmap`/`pmap_n` → `join_all` (replaces rayon scope). Builtin function type split: `BuiltinKind::Sync(SyncBuiltinFn)` / `BuiltinKind::Async(AsyncBuiltinFn)`. HOFs (map/filter/fold/etc.) return `BoxFuture`. `call_value` async; `call_value_sync` bridge (`block_in_place` + `Handle::current().block_on()`) for 31 sync stdlib files. Cron background threads use `ctx.tokio_runtime.block_on()` directly (no Handle). `reqwest::blocking` calls wrapped in `block_in_place` (HTTP backend, MCP transport). New deps: `async-recursion`, `futures`. CLI entry: `ctx.tokio_runtime.block_on()`. dx backend: direct `.await` (already async). 90/90 tests |
| 72 | 03-19 | `agent.adapter`/`negotiate_format`/`coerce` Trait format negotiation: 3 functions. `agent.adapter` creates reusable field-mapping adapter from source→target Trait with explicit mapping record. `agent.coerce` one-shot message transform with validation. `agent.negotiate_format` runtime negotiation — inspects agent capabilities, attempts exact/structural/subset Trait matching with Levenshtein distance heuristics. Returns `Err` on incompatible. Adapters return `Value::Err` on validation failure (catchable with `??`). 2 Rust files (agent_adapter.rs + agent_negotiate_fmt.rs). 91/91 tests |
| 73 | 03-19 | `agent.reload`/`evolve`/`update_traits` hot-swap agent handlers: 3 functions. `agent.reload` replaces handler externally via global AGENT_HANDLERS DashMap (ID-based mutable handler store). `agent.evolve` self-updates from within handler (thread-local PENDING_EVOLVE flag, applied by interpreter after handler returns). `agent.update_traits` adds/removes traits on agent Records. Subprocess agents return Err on reload. Interceptors preserved — `bi_next` dynamically resolves handler via store, `intercept` strips `__handler_id` so intercepted agents use their wrapped handler. `on_reload` callback receives old and new handler. 1 Rust file (agent_reload.rs). 92/92 tests |
| 74 | 03-19 | `agent.dialogue_save/load/list/delete` dialogue persistence: 4 functions. `dialogue_save` persists session state (config, turn history) to `.lx/dialogues/{id}.json` with atomic write (tmp+rename). `dialogue_load` restores session from file and binds to a (possibly different) agent. `dialogue_list` enumerates saved dialogues with metadata (id, role, turns, created, updated, context_preview). `dialogue_delete` removes saved dialogue file. Uses `json_conv` for serialization, same pattern as `std/durable`. History entries serialized with direction (outbound/inbound) and timestamps. 1 Rust file (agent_dialogue_persist.rs). 93/93 tests |
