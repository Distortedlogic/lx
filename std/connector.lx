-- Generic connector contract for external tool access.
-- MCP servers, CLI tools, HTTP APIs all implement this interface.
-- Agent code calls connector.call regardless of backend.

+Trait Connector = {
  connect: {} -> Result
  disconnect: {} -> Result
  call: {tool: Str  args: Any} -> Result
  tools: {} -> [Any]
}
