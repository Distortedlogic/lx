-- default tool: write content to file via std/fs

use std/fs

Tool Write = {
  name: "write"
  description: "Write content to file"
  params: {
    path: Str
    content: Str
  }
  run = (args) { fs.write (args.path) (args.content) }
}
