-- tell/ask operator tests
-- verifies ~> (tell) and ~>? (ask) work as infix messaging operators

f = (target msg) { try target ~> msg }
g = (target msg) { try target ~>? msg }
assert (type_of f == "Func")
assert (type_of g == "Func")
