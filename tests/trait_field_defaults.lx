-- trait field default inheritance test
-- verifies Class : [Trait] inherits field defaults without redeclaring them

Trait HasDefault = {
  x: Int = 42
  get_x = () { self.x }
}
Class Simple : [HasDefault] = {}
s = Simple {}
assert s.x == 42
assert (s.get_x ()) == 42
