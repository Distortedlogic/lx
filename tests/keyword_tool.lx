Tool Echo = {
  description: "echoes"
  params: {text: "Str"}
  run = (args) { Ok args.text }
}

t = Echo {}
assert t.description == "echoes"
r = t.run {text: "hi"}
assert r == Ok "hi"
v = t.validate {text: "hello"}
assert (v | ok?)
bad = t.validate {}
assert (bad | err?)
