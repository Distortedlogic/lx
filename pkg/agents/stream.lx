-- Agent streaming — emit_stream / end_stream for ~>>? protocol.
-- Hoisted from agent_stream.rs.

+emit_stream = (value) {
  emit {type: "stream"  value: value}
}

+end_stream = () {
  emit {type: "stream_end"}
}
