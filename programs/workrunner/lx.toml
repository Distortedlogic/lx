[package]
name = "workrunner"
version = "0.1.0"
entry = "main.lx"
description = "Agentic work item executor with per-task grading"

[backends]
llm = "claude-code"

[test]
dir = "tests"
pattern = "*.lx"
