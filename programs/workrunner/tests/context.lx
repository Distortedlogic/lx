use ../context

ctx = context.load_bootstrap ()
assert (ctx | len > 0)
assert (ctx | contains? "Context Bootstrap")
assert (ctx | contains? "Inventory")
assert (ctx | contains? "Reference")
assert (ctx | contains? "Stdlib")

log.info "context: all passed"
