-- GitHub code search specialist -- find OSS implementations.

use ../lib/github
use pkg/ai/ai_agent

+search = (msg) {
  repos = github.search msg.query (msg.lang ?? "rust")
  {repos}
}

+handlers = %{"search": search}
+main = () { ai_agent.serve handlers }
